import '../simple_logger.dart';

class LoggerOptions {
  late String pChar = "*";
  late bool enableDebug = true;
  late LogColor infoColor = LogColor.Green;
  late LogColor warningColor = LogColor.Yellow;
  late LogColor errorColor = LogColor.Red;
}
